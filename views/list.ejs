<!DOCTYPE html>
<html lang="en">
    <%- include("header") %>  
    <div class="box" id="heading">
        <h1><%=title  %></h1>
    </div>
        <div class="box">
            <%  for(var i=0; i < newItems.length; i++){  %>
                <div class="item">
                    <input type="checkbox">
                    <p><%=newItems[i] %> </p>
                </div>
                
           <%   }  %>
           <form action="/" method="POST" class="item">
            <input type="text" name="todo" placeholder="Enter new to do" autocomplete="off">
            <button type="submit" value=<%= title %> name="list">+</button>
        </form>
        </div>

<%- include("footer") %>  